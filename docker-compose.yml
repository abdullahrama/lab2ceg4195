version: "3.9"

services:
  segmentation_service:
    build: .
    container_name: ceg4195-lab2
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - .env
    volumes:
      - ./models:/app/models:ro
